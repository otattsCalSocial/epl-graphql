extend type Query {
    teams: [Team!]!
    team(id: ID!): Team
}

type Team {
    id: ID!
    name: String!
    shortName: String!
    players(limit: Int, sortBy: String): [Player!]!
    matches(season: String): [Fixture!]!
}
